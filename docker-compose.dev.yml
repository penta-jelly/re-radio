version: "3"
services:
  service:
    volumes:
      - ./server:/user/app
      - /user/app/node_modules
  client:
    volumes:
      - ./client:/usr/src/app
      - ./client/node_modules:/usr/src/app/node_modules
