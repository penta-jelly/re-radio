version: '3'
services:
  server:
    volumes:
      - .:/user/app
    command: >
      sh -c "
        wait-on http://prisma:${PRISMA_PORT} && 
        npm run prisma:deploy --force &&
        npm run dev
      "
