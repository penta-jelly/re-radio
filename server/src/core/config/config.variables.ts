export enum EnvVariables {
  NODE_ENV,

  RADIO_SERVER_PORT,

  DB_HOST,
  DB_PORT,
  DB_USER,
  DB_PASSWORD,
  DB_NAME,

  REDIS_HOST,
  REDIS_PORT,

  JWT_SECRET,
  JWT_TOKEN_EXPIRES_IN,

  YOUTUBE_API_URL,
  YOUTUBE_API_KEY,
}
