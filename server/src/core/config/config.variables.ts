export enum EnvVariables {
  NODE_ENV,

  LOG_LEVEL,
  LOG_UNCOLORIZE,

  RADIO_SERVER_HOST,
  RADIO_SERVER_PORT,
  RADIO_REAL_TIME_SERVER_HOST,
  RADIO_REAL_TIME_SERVER_PORT,

  RADIO_SERVER_USER_INACTIVE_TIMEOUT,

  DB_HOST,
  DB_PORT,
  DB_USER,
  DB_PASSWORD,
  DB_NAME,

  REDIS_HOST,
  REDIS_PORT,

  JWT_SECRET,
  JWT_TOKEN_EXPIRES_IN,

  YOUTUBE_API_URL,
  YOUTUBE_API_KEY,
}
